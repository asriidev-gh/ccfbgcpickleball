/** Spectator notification bell — keep polling light on shared Atlas tiers. */
export const ORGANIZER_NOTIFICATIONS_POLL_MS = 60_000;

/** Operator dashboard: refresh queue while open play is live. */
export const OPERATOR_QUEUE_LIVE_POLL_MS = 5_000;

/** Spectator live view: queue + courts. */
export const SPECTATOR_LIVE_POLL_MS = 60 * 1000;
